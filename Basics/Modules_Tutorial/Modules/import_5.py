# when you import a file, you run everything in it
# never use import * it is not recommended
from my_module import *


courses = ["History", "Math", "Physics", "CompSci"]

index = find_index(courses, "Math")
print(index)

print(test)
